# Demo Showcase

App Angular standalone para validar visualmente o componente `pfu-dynamic-form` usando a biblioteca local via alias TypeScript.

## Comandos

```bash
npm install
npm run start:demo
```

Para gerar build de producao:

```bash
npm run build:demo
```
